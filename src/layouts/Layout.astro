---
import { SEO } from "astro-seo";
import { AstroFont } from "astro-font";
import { join } from "node:path";
import { Header, MobileHeader, Cursor } from "../components/UI";
import { Footer } from "../components";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<meta name="theme-color" content="#040608" />
		<meta name="robots" content="index, follow" />
		<meta
			name="google-site-verification"
			content="YRAUUyc8TP4QJ1s53KAdLxcON9xifQf33BnLeP-F_5Y"
		/>
		<meta
			name="keywords"
			content="The Hotel Kyiv, luxury hotel Kyiv, five-star suites, rooftop bar Kyiv, spa Kyiv, Ukrainian hospitality"
		/>
		<meta name="author" content="The Hotel Kyiv" />
		<meta name="publisher" content="The Hotel Kyiv" />
		<link rel="manifest" href="/manifest.json" />

		<!-- Preload CSS Styles -->
		<link rel="preload stylesheet" href="/styles/global.css" as="style" />
		<link rel="preload stylesheet" href="/styles/utility.css" as="style" />
		<link rel="preload stylesheet" href="/styles/cursor.css" as="style" />

		<SEO
			title="The Hotel Kyiv | Five-Star Luxury Above the Dnipro"
			description="Discover luminous suites, Michelin-caliber dining, and holistic wellness rituals at The Hotel Kyiv, a five-star destination above the Dnipro."
			canonical="https://thehotelkyiv.com/"
			openGraph={{
				basic: {
					title: "The Hotel Kyiv | Five-Star Luxury Above the Dnipro",
					type: "website",
					image: "https://images.unsplash.com/photo-1484154218962-a197022b5858?auto=format&fit=crop&w=1200&q=80",
					url: "https://thehotelkyiv.com/",
				},
			}}
			twitter={{
				title: "The Hotel Kyiv | Five-Star Luxury Above the Dnipro",
				description:
					"Discover luminous suites, Michelin-caliber dining, and holistic wellness rituals in the heart of Ukraine.",
				card: "summary",
				site: "@TheHotelKyiv",
				creator: "@TheHotelKyiv",
				image: "https://images.unsplash.com/photo-1484154218962-a197022b5858?auto=format&fit=crop&w=1200&q=80",
			}}
			extend={{
				link: [
					{ rel: "icon", type: "image/png", href: "/favicon.png" },
					{
						rel: "shortcut",
						type: "image/png",
						href: "/favicon.png",
					},
					{
						rel: "apple-touch-icon",
						type: "image/png",
						href: "/favicon.png",
					},
				],
				meta: [
					{
						name: "twitter:url",
						content: "https://thehotelkyiv.com/",
					},
				],
			}}
		/>

		<AstroFont
			config={[
				{
					name: "Poppins",
					src: [
						{
							weight: "400",
							style: "normal",
							path: join(
								process.cwd(),
								"public",
								"fonts",
								"poppins-v20-latin-regular.woff2",
							),
						},
					],
					preload: true,
					display: "swap",
					fallback: "serif",
					cssVariable: "astro-font-poppins",
				},
				{
					name: "Satisfy",
					src: [
						{
							weight: "400",
							style: "normal",
							path: join(
								process.cwd(),
								"public",
								"fonts",
								"satisfy-v17-latin-regular.woff2",
							),
						},
					],
					preload: true,
					display: "swap",
					fallback: "serif",
					cssVariable: "astro-font-satisfy",
				},
				{
					name: "Ubuntu",
					src: [
						{
							weight: "700",
							style: "normal",
							path: join(
								process.cwd(),
								"public",
								"fonts",
								"ubuntu-v20-latin-700.woff2",
							),
						},
					],
					preload: true,
					display: "swap",
					fallback: "serif",
					cssVariable: "astro-font-ubuntu",
				},
			]}
		/>

		<style is:global>
			:root {
				--base: #040608;
				--baseLite: rgba(4, 6, 8, 0.9);
				--text: #f7f4ee;

				--pH: 38;
				--pS: 65%;
				--pL: 62%;

				--sH: 210;
				--sS: 30%;
				--sL: 32%;

				--mH: 26;
				--mS: 70%;
				--mL: 52%;

				--primary: var(--pH), var(--pS), var(--pL);
				--secondary: var(--sH), var(--sS), var(--sL);
				--mixColor: var(--mH), var(--mS), var(--mL);

				--primaryLite: hsla(var(--primary), 0.7);
				--primaryLiteAlt: hsla(var(--primary), 0.35);
				--secondaryLite: hsla(var(--secondary), 0.6);
				--secondaryLiteAlt: hsla(var(--secondary), 0.35);

				--footer: #050910;
				--footerSvgA: #070c12;
				--footerSvgB: #0a1119;

				--gradient: linear-gradient(
					110deg,
					hsl(var(--secondary)),
					hsl(var(--primary))
				);
				--gradientLite: linear-gradient(
					45deg,
					var(--primaryLite),
					hsl(var(--secondaryLite))
				);

				--headerGradient: linear-gradient(
					180deg,
					rgba(4, 6, 8, 0.9),
					transparent
				);
				--cardGradient: linear-gradient(
					45deg,
					hsla(210, 40%, 8%, 0.9),
					var(--primaryLiteAlt)
				);
				--cardGradientAlt: linear-gradient(
					-135deg,
					hsla(210, 25%, 12%, 0.9),
					var(--primaryLite)
				);
				--miscCardGradient: linear-gradient(
					-45deg,
					hsla(210, 35%, 8%, 0.8),
					var(--primaryLiteAlt)
				);
				--overlayGradient: radial-gradient(
					farthest-corner at 100% 0%,
					hsla(210, 40%, 8%, 0.6),
					transparent
				);
				--LinkBtnGradient: linear-gradient(
					-135deg,
					hsla(var(--secondary), 0.5),
					hsla(var(--primary), 0.4)
				);
				--SkillGradient: linear-gradient(
					-135deg,
					hsla(210, 20%, 15%, 0.85),
					hsla(var(--primary), 0.25)
				);
				--SkillGradientAlt: linear-gradient(
					-135deg,
					hsla(var(--primary), 0.3),
					hsla(var(--secondary), 0.35)
				);
				--orbitGradient: conic-gradient(
					transparent,
					hsl(var(--primary)),
					hsl(var(--secondary)),
					#fff
				);
				--orbitGradientAlt: conic-gradient(
					#fff,
					hsl(var(--primary)),
					hsl(var(--secondary)),
					transparent,
					transparent
				);

				--orbitDotColor: #f7f4ee;
				--heroSocialColor: #f7f4ee;
				--contactSocialColor: hsla(0, 0%, 100%, 0.4);

				--cursorColor: hsla(var(--primary), 0.6);
				--cursorColorAlt: hsla(var(--primary), 0.8);
				--dotColor: #f7f4ee;
			}

			html.light {
				--base: #faf7f0;
				--baseLite: rgba(250, 247, 240, 0.9);
				--text: #1a1c1f;

				--pH: 38;
				--pS: 65%;
				--pL: 45%;

				--sH: 210;
				--sS: 35%;
				--sL: 32%;

				--mH: 26;
				--mS: 60%;
				--mL: 40%;

				--primary: var(--pH), var(--pS), var(--pL);
				--secondary: var(--sH), var(--sS), var(--sL);
				--mixColor: var(--mH), var(--mS), var(--mL);

				--primaryLite: hsla(var(--primary), 0.5);
				--primaryLiteAlt: hsla(var(--primary), 0.25);
				--secondaryLite: hsla(var(--secondary), 0.3);
				--secondaryLiteAlt: hsla(var(--secondary), 0.2);

				--footer: hsl(var(--primary), 0.25);
				--footerSvgA: hsl(var(--primary), 0.3);
				--footerSvgB: hsl(var(--primary), 0.4);

				--gradient: linear-gradient(
					to right,
					hsl(var(--secondary)),
					hsl(var(--primary))
				);
				--gradientLite: linear-gradient(
					45deg,
					var(--primaryLite),
					hsl(var(--secondaryLite))
				);

				--headerGradient: linear-gradient(
					180deg,
					rgba(250, 247, 240, 0.9),
					transparent
				);
				--cardGradient: linear-gradient(
					45deg,
					hsla(var(--secondary), 0.08),
					var(--primaryLiteAlt)
				);
				--cardGradientAlt: linear-gradient(
					-135deg,
					var(--primaryLiteAlt),
					hsl(0, 0%, 100%)
				);
				--miscCardGradient: linear-gradient(
					-45deg,
					var(--primaryLiteAlt),
					hsl(0, 0%, 100%)
				);
				--overlayGradient: radial-gradient(
					farthest-corner at 100% 0%,
					hsla(0, 0%, 100%, 0.4),
					transparent
				);
				--LinkBtnGradient: linear-gradient(
					-135deg,
					hsla(var(--secondary), 0.15),
					hsla(var(--mixColor), 0.15)
				);
				--SkillGradient: linear-gradient(
					-135deg,
					hsla(var(--mixColor), 0.125),
					hsla(var(--primary), 0.15)
				);
				--SkillGradientAlt: linear-gradient(
					-135deg,
					hsla(var(--primary), 0.15),
					hsla(var(--secondary), 0.15)
				);
				--orbitGradient: conic-gradient(
					transparent,
					hsl(var(--secondary)),
					hsl(var(--primary))
				);
				--orbitGradientAlt: conic-gradient(
					hsl(var(--primary)),
					hsl(var(--secondary)),
					transparent,
					transparent
				);

				--orbitDotColor: hsl(var(--secondary));
				--heroSocialColor: hsl(var(--secondary));
				--contactSocialColor: hsla(var(--secondary), 0.4);

				--cursorColor: hsla(var(--primary), 0.6);
				--cursorColorAlt: hsla(var(--primary), 0.6);
				--dotColor: hsl(var(--secondary));
			}

			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			html {
				font-family: Poppins !important;
			}

			body {
				background-color: var(--base);
				color: var(--text);
			}

			/* Header */
			.Header_Nav a {
				position: relative;
				overflow: hidden;
				letter-spacing: 1px;
			}

			.Header_Nav a::after {
				content: "";
				position: absolute;
				bottom: 0;
				left: 0;
				width: 100%;
				height: 2px;
				background-color: var(--orbitDotColor);
				transform: scaleX(0);
				transition: transform 0.25s ease-out;
			}

			.Header_Nav a.active::after {
				transform: scaleX(1);
			}

			/* Action Button Style */
			.Header_ActionBtn {
				position: relative;
				border-radius: 0.2em;
				overflow: hidden;
				display: grid;
				place-items: center;
			}

			.Header_ActionBtn::after {
				content: "";
				position: absolute;
				width: 120%;
				aspect-ratio: 1;
				background: conic-gradient(
					hsl(var(--primary)),
					hsl(var(--secondary)),
					hsl(var(--primary))
				);
				animation: spin 5s linear infinite;
			}

			.Header_ActionBtn > div {
				padding: 0.2em 1.35em;
				background-color: var(--base);
				color: var(--text);
				border-radius: inherit;
				margin: 2px;
				z-index: 2;
			}

			/* Mobile Header Nav */
			.MobileHeader_Nav {
				min-width: 14em;
				text-align: center;
				background-color: var(--base);
				border-radius: 0.3em;
				padding: 1em;
				font-size: 1.3em;
				font-weight: 700;
				letter-spacing: 1px;
			}

			.MobileHeader_Nav a {
				width: 100%;
				color: var(--text);
				text-decoration: none;
				text-transform: uppercase;
			}

			.MobileHeader_Nav a.active {
				background: var(--gradient);
				background-clip: text;
				-webkit-background-clip: text;
				color: transparent;
				background-size: 200%;
				animation: gradientPan 5s linear infinite;
			}

			:has(.MobileHeader_ActionBtn).MobileHeader_Nav {
				padding: 0;
			}

			.MobileHeader_ActionBtn {
				position: relative;
				border-radius: 0.2em;
				overflow: hidden;
			}

			.MobileHeader_ActionBtn::after {
				content: "";
				position: absolute;
				width: 120%;
				aspect-ratio: 1;
				background: conic-gradient(
					hsl(var(--primary)),
					hsl(var(--secondary)),
					hsl(var(--primary))
				);
				animation: spin 5s linear infinite;
			}

			.MobileHeader_ActionBtn > div {
				padding: 0.7em 1em;
				width: 100%;
				background-color: var(--base);
				color: var(--text);
				border-radius: inherit;
				margin: 4px;
				z-index: 2;
			}

			/* Page Title Style */
			.Title-Tag {
				font-family: Ubuntu;
			}

			.Title-Tag span,
			.Title-Tag p {
				z-index: 1;
				background-color: var(--base);
				padding-inline: 0.25em;
			}

			.Title-Tag.open {
				margin-bottom: 1em;
				justify-content: flex-start;
			}

			.Title-Tag.close {
				justify-content: flex-end;
				padding-top: 0.5em;
				opacity: 0.7 !important;
			}

			.Title-Tag.primary span {
				color: hsl(var(--primary));
			}

			.Title-Tag.secondary span {
				color: hsl(var(--secondary));
			}

			.Title_Line::after {
				content: "";
				position: absolute;
				margin-top: 0.3em;
				width: 100%;
				height: 2px;
				z-index: 0;
				background-color: var(--primaryLite);
			}

			.Title_Line.secondary::after {
				background-color: var(--secondaryLite);
			}

			.Title_Line.close::after {
				background-color: transparent;
			}

			/* Mobile Ham Menu Animation --> */
			.MobileNav_Ham {
				-webkit-tap-highlight-color: transparent;
				transition: transform 0.4s;
				user-select: none;
			}

			.MobileNav_Ham .line {
				fill: none;
				transition:
					stroke-dasharray 0.4s,
					stroke-dashoffset 0.4s;
				stroke: var(--text);
				stroke-width: 5.5;
				transform-origin: right;
				stroke-linecap: round;
			}

			html.light .MobileNav_Ham .line {
				stroke: hsl(var(--primary)) !important;
			}

			.MobileNav_Ham .top {
				stroke-dasharray: 18 160;
			}

			.MobileNav_Ham .middle {
				stroke-dasharray: 40 142;
				transform-origin: 83% 83%;
				transition: transform 0.4s;
			}

			.MobileNav_Ham .bottom {
				stroke-dasharray: 18 85;
				transform-origin: 50%;
				transition:
					transform 0.4s,
					stroke-dashoffset 0.4s;
			}

			.Projects_Link {
				padding: 0.45em;
				border: 1px solid var(--primaryLite);
				border-radius: 100%;
				z-index: 10;
			}

			/* Cursor */
			.Cursor-Main {
				position: absolute;
				width: 100%;
				height: 100%;
				inset: 0;
				z-index: 9999;
				overflow: hidden;
				pointer-events: none;
			}

			/* Hero */
			.Hero_Trail {
				position: absolute;
				aspect-ratio: 1;
				border-radius: 50%;
				animation: spin 6s linear infinite;
				animation-delay: 1s;
			}

			.Hero_Trail::before {
				content: "";
				width: calc(100% - 1%);
				aspect-ratio: 1;
				inset: 0;
				margin: auto;
				border-radius: 50%;
				position: absolute;
				background-color: var(--base);
			}

			.Trail1 {
				width: 60%;
				background: var(--orbitGradient);
				z-index: 5;
			}

			.Trail2 {
				width: 70%;
				animation-duration: 8s;
				animation-direction: reverse;
				background: var(--orbitGradientAlt);
				z-index: 4;
			}

			.Trail3 {
				width: 80%;
				background: var(--orbitGradient);
				animation-duration: 10s;
				z-index: 3;
			}

			.Trail4 {
				width: 90%;
				animation-duration: 12s;
				animation-direction: reverse;
				background: var(--orbitGradientAlt);
				z-index: 2;
			}

			.Trail2 span,
			.Trail4 span {
				transform: rotate(140deg) !important;
			}

			footer {
				color: #fff !important;
			}

			/* Lenis Smooth Scroll */
			html.lenis {
				height: auto;
			}

			.lenis.lenis-smooth {
				scroll-behavior: auto !important;
			}

			.lenis.lenis-smooth [data-lenis-prevent] {
				overscroll-behavior: contain;
			}

			.lenis.lenis-stopped {
				overflow: hidden;
			}

			.lenis.lenis-scrolling iframe {
				pointer-events: none;
			}

			/* GSAP Animation inits */
			.flex_center {
				display: flex;
				justify-content: center;
				align-items: center;
			}

			.gradientText {
				background: var(--gradient);
				background-clip: text;
				-webkit-background-clip: text;
				color: transparent;
				background-size: 200%;
				animation: gradientPan 5s linear infinite;
				will-change: background-position;
			}

			.Orbit_Dot {
				width: 0.5em;
				aspect-ratio: 1;
				background-color: var(--orbitDotColor);
				border-radius: 0 50% 50%;
				transform: rotate(-45deg);
				position: absolute;
				top: -0.2em;
				left: 49%;
				transition: 0.25s ease-in-out;
				overflow: hidden;
				box-shadow: 0 0 10px var(--orbitDotColor);
			}

			:has(.Hero_Orbit:hover) .Orbit_Dot {
				background-color: var(--base) !important;
				outline: 0.5px solid var(--orbitDotColor);
				/* border-radius: 50% !important; */
				scale: 1.8;
			}

			.Fade_Stagger,
			.Fade_Up {
				opacity: 0;
				transform: translateY(50);
			}

			.Fade_Down {
				opacity: 0;
				transform: translateY(-50);
			}

			.Fade_In {
				opacity: 0;
			}

			.Spring_Up {
				opacity: 0;
				scale: 0.75;
			}

			@keyframes spin {
				0% {
					rotate: 0deg;
				}

				100% {
					rotate: 360deg;
				}
			}
		</style>
	</head>
	<body id="home">
		<Header />
		<MobileHeader />

		<main>
			<slot />
		</main>

		<Footer />
		<Cursor />
		<script src="../script.ts"></script>
	</body>
</html>
